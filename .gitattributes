# Normalise to LF in the repository. CI runs on Ubuntu; without this every file
# committed from Windows shows as fully rewritten on the first Linux checkout.
* text=auto eol=lf
*.csv text eol=lf
*.png binary
*.xlsx binary
*.pkl binary
